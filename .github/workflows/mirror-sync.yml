name: 同步仓库和镜像

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'auto-update.sh'
      - 'auto-setup'
      - 'third-party-installer.sh'
  workflow_run:
    workflows: ["同步上游发布版本"]
    types:
      - completed
  schedule:
    - cron: '30 19 * * *'

env:
  SOURCE_REPO: whzhni1/ipk_apk
  GITEE_USERNAME: whzhni
  GITCODE_USERNAME: whzhni
  REPO: ipk_apk
  BRANCH: main
  FILES_TO_SYNC: 'auto-update.sh auto-setup third-party-installer.sh'

jobs:
  # ========================================
  # 阶段1: 同步文件到 GitHub
  # ========================================
  sync-files-to-github:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    outputs:
      files_changed: ${{ steps.sync.outputs.files_changed }}
      changed_list: ${{ steps.sync.outputs.changed_list }}
      duration: ${{ steps.sync.outputs.duration }}
      configured: ${{ steps.sync.outputs.configured }}
    
    steps:
      - name: 检出本仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          path: source

      - name: 检出目标仓库
        uses: actions/checkout@v4
        with:
          repository: ${{ env.SOURCE_REPO }}
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0
          path: target

      - name: 配置 Git
        run: |
          cd target
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 同步文件
        id: sync
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          START_TIME=$(date +%s)
          
          # 检查 PAT_TOKEN 配置
          if [ -z "$PAT_TOKEN" ]; then
            echo "❌ PAT_TOKEN 未设置"
            echo "configured=未设置" >> $GITHUB_OUTPUT
            echo "duration=0" >> $GITHUB_OUTPUT
            echo "files_changed=false" >> $GITHUB_OUTPUT
            exit 1
          fi
          
          echo "configured=已设置" >> $GITHUB_OUTPUT
          
          echo "========================================="
          echo "📋 同步文件到 ${{ env.SOURCE_REPO }}"
          echo "========================================="
          echo ""
          
          # 显示源文件信息
          echo "📁 源文件列表："
          cd source
          for file in ${{ env.FILES_TO_SYNC }}; do
            if [ -f "$file" ]; then
              size=$(ls -lh "$file" | awk '{print $5}')
              md5=$(md5sum "$file" | awk '{print $1}')
              echo "  ✅ $file ($size) [MD5: ${md5:0:8}...]"
            else
              echo "  ❌ $file (不存在)"
            fi
          done
          
          echo ""
          echo "📁 目标文件列表（同步前）："
          cd ../target
          for file in ${{ env.FILES_TO_SYNC }}; do
            if [ -f "$file" ]; then
              size=$(ls -lh "$file" | awk '{print $5}')
              md5=$(md5sum "$file" | awk '{print $1}')
              echo "  📄 $file ($size) [MD5: ${md5:0:8}...]"
            else
              echo "  ⚠️  $file (不存在)"
            fi
          done
          
          echo ""
          echo "🔄 开始复制文件..."
          
          # 复制文件
          for file in ${{ env.FILES_TO_SYNC }}; do
            if [ -f "../source/$file" ]; then
              cp -f "../source/$file" "./$file"
              echo "  ✅ 已复制: $file"
            else
              echo "  ❌ 跳过: $file (源文件不存在)"
            fi
          done
          
          echo ""
          echo "🔍 检查变更..."
          
          # 检查是否有变更
          git add ${{ env.FILES_TO_SYNC }} 2>/dev/null || true
          
          if git diff --cached --quiet; then
            echo "ℹ️  没有检测到文件变更"
            echo "files_changed=false" >> $GITHUB_OUTPUT
            echo "changed_list=" >> $GITHUB_OUTPUT
            
            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))
            echo "duration=$DURATION" >> $GITHUB_OUTPUT
          else
            echo "✅ 检测到文件变更"
            
            # 获取变更的文件列表
            CHANGED_FILES=$(git diff --cached --name-only)
            echo ""
            echo "📝 变更文件："
            echo "$CHANGED_FILES" | while read file; do
              echo "  - $file"
            done
            
            # 显示详细差异
            echo ""
            echo "📊 文件差异："
            git diff --cached --stat
            
            echo ""
            echo "files_changed=true" >> $GITHUB_OUTPUT
            echo "changed_list<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGED_FILES" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            
            # 提交更改
            git commit -m "🔄 同步工具脚本更新

            变更文件：
            $CHANGED_FILES

            - 来源仓库: ${{ github.repository }}
            - 提交 SHA: ${{ github.sha }}
            - 提交信息: ${{ github.event.head_commit.message }}
            - 更新时间: $(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S') (北京时间)"
            
            echo ""
            echo "🚀 推送到 ${{ env.SOURCE_REPO }}..."
            
            # 推送并显示结果
            if git push origin ${{ env.BRANCH }}; then
              echo "✅ 推送成功"
              
              # 显示推送后的文件状态
              echo ""
              echo "📁 目标文件列表（同步后）："
              for file in ${{ env.FILES_TO_SYNC }}; do
                if [ -f "$file" ]; then
                  size=$(ls -lh "$file" | awk '{print $5}')
                  md5=$(md5sum "$file" | awk '{print $1}')
                  echo "  ✅ $file ($size) [MD5: ${md5:0:8}...]"
                fi
              done
            else
              echo "❌ 推送失败"
              exit 1
            fi
            
            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))
            echo "duration=$DURATION" >> $GITHUB_OUTPUT
          fi
          
          echo ""
          echo "========================================="
          echo "✅ GitHub 同步完成"
          echo "========================================="

  # ========================================
  # 阶段2: 同步到 Gitee
  # ========================================
  sync-to-gitee:
    runs-on: ubuntu-latest
    needs: [sync-files-to-github]
    if: |
      always() && 
      !contains(needs.*.result, 'failure') && 
      !contains(needs.*.result, 'cancelled') &&
      (github.event_name == 'workflow_dispatch' || 
       github.event_name == 'schedule' || 
       github.event_name == 'push' ||
       (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success'))
    outputs:
      status: ${{ steps.sync.outcome }}
      duration: ${{ steps.sync.outputs.duration }}
      configured: ${{ steps.sync.outputs.configured }}
    
    steps:
      - name: 等待 GitHub 同步完成
        if: github.event_name == 'push' && needs.sync-files-to-github.outputs.files_changed == 'true'
        run: |
          echo "⏳ 等待10秒确保 GitHub 同步完全生效..."
          sleep 10

      - name: 检出源仓库
        uses: actions/checkout@v4
        with:
          repository: ${{ env.SOURCE_REPO }}
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}

      - name: 配置 Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 同步到 Gitee
        id: sync
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          START_TIME=$(date +%s)
          
          if [ -z "$GITEE_TOKEN" ]; then
            echo "⚠️ GITEE_TOKEN 未设置"
            echo "configured=未设置" >> $GITHUB_OUTPUT
            echo "duration=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "configured=已设置" >> $GITHUB_OUTPUT
          
          echo "========================================="
          echo "📋 源仓库: ${{ env.SOURCE_REPO }}"
          echo "🎯 目标: https://gitee.com/${{ env.GITEE_USERNAME }}/${{ env.REPO }}"
          echo "========================================="
          
          git remote add gitee https://oauth2:${GITEE_TOKEN}@gitee.com/${{ env.GITEE_USERNAME }}/${{ env.REPO }}.git 2>/dev/null || \
          git remote set-url gitee https://oauth2:${GITEE_TOKEN}@gitee.com/${{ env.GITEE_USERNAME }}/${{ env.REPO }}.git
          
          echo "🚀 开始推送..."
          git push gitee +${{ env.BRANCH }}:${{ env.BRANCH }}
          
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "duration=$DURATION" >> $GITHUB_OUTPUT
          
          echo "✅ Gitee 同步完成 (耗时 ${DURATION}s)"

  # ========================================
  # 阶段3: 同步到 GitCode
  # ========================================
  sync-to-gitcode:
    runs-on: ubuntu-latest
    needs: [sync-files-to-github]
    if: |
      always() && 
      !contains(needs.*.result, 'failure') && 
      !contains(needs.*.result, 'cancelled') &&
      (github.event_name == 'workflow_dispatch' || 
       github.event_name == 'schedule' || 
       github.event_name == 'push' ||
       (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success'))
    outputs:
      status: ${{ steps.sync.outcome }}
      duration: ${{ steps.sync.outputs.duration }}
      configured: ${{ steps.sync.outputs.configured }}
    
    steps:
      - name: 等待 GitHub 同步完成
        if: github.event_name == 'push' && needs.sync-files-to-github.outputs.files_changed == 'true'
        run: |
          echo "⏳ 等待10秒确保 GitHub 同步完全生效..."
          sleep 10

      - name: 检出源仓库
        uses: actions/checkout@v4
        with:
          repository: ${{ env.SOURCE_REPO }}
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}

      - name: 配置 Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 同步到 GitCode
        id: sync
        env:
          GITCODE_TOKEN: ${{ secrets.GITCODE_TOKEN }}
        run: |
          START_TIME=$(date +%s)
          
          if [ -z "$GITCODE_TOKEN" ]; then
            echo "⚠️ GITCODE_TOKEN 未设置"
            echo "configured=未设置" >> $GITHUB_OUTPUT
            echo "duration=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "configured=已设置" >> $GITHUB_OUTPUT
          
          echo "========================================="
          echo "📋 源仓库: ${{ env.SOURCE_REPO }}"
          echo "🎯 目标: https://gitcode.com/${{ env.GITCODE_USERNAME }}/${{ env.REPO }}"
          echo "========================================="
          
          git remote add gitcode https://oauth2:${GITCODE_TOKEN}@gitcode.com/${{ env.GITCODE_USERNAME }}/${{ env.REPO }}.git 2>/dev/null || \
          git remote set-url gitcode https://oauth2:${GITCODE_TOKEN}@gitcode.com/${{ env.GITCODE_USERNAME }}/${{ env.REPO }}.git
          
          echo "🚀 开始推送..."
          git push gitcode +${{ env.BRANCH }}:${{ env.BRANCH }}
          
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "duration=$DURATION" >> $GITHUB_OUTPUT
          
          echo "✅ GitCode 同步完成 (耗时 ${DURATION}s)"

  # ========================================
  # 阶段4: 生成报告
  # ========================================
  report:
    runs-on: ubuntu-latest
    needs: [sync-files-to-github, sync-to-gitee, sync-to-gitcode]
    if: always()
    
    steps:
      - name: 生成同步报告
        run: |
          GITHUB_SYNC_STATUS="${{ needs.sync-files-to-github.result }}"
          FILES_CHANGED="${{ needs.sync-files-to-github.outputs.files_changed }}"
          CHANGED_LIST="${{ needs.sync-files-to-github.outputs.changed_list }}"
          GITHUB_DURATION="${{ needs.sync-files-to-github.outputs.duration }}"
          GITHUB_CONFIGURED="${{ needs.sync-files-to-github.outputs.configured }}"
          GITEE_STATUS="${{ needs.sync-to-gitee.result }}"
          GITCODE_STATUS="${{ needs.sync-to-gitcode.result }}"
          GITEE_CONFIGURED="${{ needs.sync-to-gitee.outputs.configured }}"
          GITCODE_CONFIGURED="${{ needs.sync-to-gitcode.outputs.configured }}"
          GITEE_DURATION="${{ needs.sync-to-gitee.outputs.duration }}"
          GITCODE_DURATION="${{ needs.sync-to-gitcode.outputs.duration }}"
          
          # 设置默认值
          GITHUB_DURATION=${GITHUB_DURATION:-0}
          GITEE_DURATION=${GITEE_DURATION:-0}
          GITCODE_DURATION=${GITCODE_DURATION:-0}
          GITHUB_CONFIGURED=${GITHUB_CONFIGURED:-未设置}
          GITEE_CONFIGURED=${GITEE_CONFIGURED:-未设置}
          GITCODE_CONFIGURED=${GITCODE_CONFIGURED:-未设置}
          FILES_CHANGED=${FILES_CHANGED:-false}
          
          # 判断 GitHub 同步状态
          if [ "${{ github.event_name }}" == "push" ]; then
            if [ "$GITHUB_SYNC_STATUS" == "success" ]; then
              if [ "$FILES_CHANGED" == "true" ]; then
                GITHUB_ICON="✅"
                GITHUB_TEXT="已更新"
              else
                GITHUB_ICON="ℹ️"
                GITHUB_TEXT="无变更"
              fi
            elif [ "$GITHUB_SYNC_STATUS" == "skipped" ]; then
              GITHUB_ICON="⏭️"
              GITHUB_TEXT="跳过"
            elif [ "$GITHUB_SYNC_STATUS" == "failure" ]; then
              GITHUB_ICON="❌"
              GITHUB_TEXT="失败"
            else
              GITHUB_ICON="❓"
              GITHUB_TEXT="未知"
            fi
          else
            GITHUB_ICON="➖"
            GITHUB_TEXT="未执行"
            GITHUB_DURATION="-"
            GITHUB_CONFIGURED="-"
          fi
          
          # 判断 Gitee 状态
          if [ "$GITEE_CONFIGURED" == "未设置" ]; then
            GITEE_ICON="⚙️"
            GITEE_TEXT="未配置"
          elif [ "$GITEE_STATUS" == "success" ]; then
            GITEE_ICON="✅"
            GITEE_TEXT="成功"
          elif [ "$GITEE_STATUS" == "skipped" ]; then
            GITEE_ICON="⏭️"
            GITEE_TEXT="跳过"
          else
            GITEE_ICON="❌"
            GITEE_TEXT="失败"
          fi
          
          # 判断 GitCode 状态
          if [ "$GITCODE_CONFIGURED" == "未设置" ]; then
            GITCODE_ICON="⚙️"
            GITCODE_TEXT="未配置"
          elif [ "$GITCODE_STATUS" == "success" ]; then
            GITCODE_ICON="✅"
            GITCODE_TEXT="成功"
          elif [ "$GITCODE_STATUS" == "skipped" ]; then
            GITCODE_ICON="⏭️"
            GITCODE_TEXT="跳过"
          else
            GITCODE_ICON="❌"
            GITCODE_TEXT="失败"
          fi
          
          # 触发方式
          if [ "${{ github.event_name }}" == "workflow_run" ]; then
            TRIGGER="主工作流完成"
          elif [ "${{ github.event_name }}" == "schedule" ]; then
            TRIGGER="定时任务"
          elif [ "${{ github.event_name }}" == "push" ]; then
            TRIGGER="文件改动"
          else
            TRIGGER="手动触发"
          fi
          
          # 生成报告
          cat >> $GITHUB_STEP_SUMMARY << EOF
          # 🔄 同步报告
          
          **触发方式**: $TRIGGER  
          **触发时间**: $(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S') (北京时间)  
          **分支**: \`${{ env.BRANCH }}\`
          
          ---
          
          ### 📋 同步结果
          
          | 同步目标 | 同步状态 | 同步用时 | 配置状态 |
          |---------|---------|---------|----------|
          | GitHub 源仓库 | $GITHUB_ICON $GITHUB_TEXT | ${GITHUB_DURATION}s | $GITHUB_CONFIGURED |
          | Gitee 镜像 | $GITEE_ICON $GITEE_TEXT | ${GITEE_DURATION}s | $GITEE_CONFIGURED |
          | GitCode 镜像 | $GITCODE_ICON $GITCODE_TEXT | ${GITCODE_DURATION}s | $GITCODE_CONFIGURED |
          
          ---
          
          ### 🌐 仓库地址
          
          - 🐙 **GitHub**: https://github.com/${{ env.SOURCE_REPO }}
          - 🟠 **Gitee**: https://gitee.com/${{ env.GITEE_USERNAME }}/${{ env.REPO }}
          - 🔵 **GitCode**: https://gitcode.com/${{ env.GITCODE_USERNAME }}/${{ env.REPO }}
          
          ---
          
          ### 📁 同步文件
          
          EOF
          
          if [ "${{ github.event_name }}" == "push" ]; then
            if [ "$FILES_CHANGED" == "true" ] && [ -n "$CHANGED_LIST" ]; then
              echo "**变更文件：**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "$CHANGED_LIST" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "**所有监控文件：**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              for file in ${{ env.FILES_TO_SYNC }}; do
                echo "- \`$file\`" >> $GITHUB_STEP_SUMMARY
              done
            else
              echo "**监控文件：**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              for file in ${{ env.FILES_TO_SYNC }}; do
                echo "- \`$file\`" >> $GITHUB_STEP_SUMMARY
              done
            fi
          else
            echo "_本次触发不涉及文件同步_" >> $GITHUB_STEP_SUMMARY
          fi
          
          # 配置提示
          if [ "$GITHUB_CONFIGURED" == "未设置" ] || [ "$GITEE_CONFIGURED" == "未设置" ] || [ "$GITCODE_CONFIGURED" == "未设置" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "---" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### ⚙️ 配置提示" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            if [ "$GITHUB_CONFIGURED" == "未设置" ]; then
              echo "- **GitHub**: 需在仓库 Settings → Secrets 中添加 \`PAT_TOKEN\`" >> $GITHUB_STEP_SUMMARY
            fi
            if [ "$GITEE_CONFIGURED" == "未设置" ]; then
              echo "- **Gitee**: 需在仓库 Settings → Secrets 中添加 \`GITEE_TOKEN\`" >> $GITHUB_STEP_SUMMARY
            fi
            if [ "$GITCODE_CONFIGURED" == "未设置" ]; then
              echo "- **GitCode**: 需在仓库 Settings → Secrets 中添加 \`GITCODE_TOKEN\`" >> $GITHUB_STEP_SUMMARY
            fi
          fi
          
          # Annotation
          echo "::notice title=同步报告::触发: $TRIGGER | GitHub: $GITHUB_ICON (${GITHUB_DURATION}s) | Gitee: $GITEE_ICON (${GITEE_DURATION}s) | GitCode: $GITCODE_ICON (${GITCODE_DURATION}s)"
